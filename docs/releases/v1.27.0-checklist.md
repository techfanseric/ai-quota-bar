# v1.27.0 release record

- User authorized commit, push and formal release on 2026-09-24.
- Version 1.27.0, macOS build 55; existing ad-hoc Apple Silicon distribution retained.
- Scope: left-click menu provider sections collapse/expand with "Follow running apps" (manual toggle on the header row, state remembered across menu reopens and restarts) instead of being removed; launch Keychain authorizations reduced to a single item (Kimi desktop safeStorage key cached in our credential vault after one interactive approval, background refresh never touches other apps' items, legacy items only probed while no vault exists and deleted after migration); Kimi monthly "Total usage" bar segmented by day with hover date labels and pace (reserve/deficit) indicators derived from a one-calendar-month display window.
- Validation: full Swift test suite passed (418 tests, 4 skipped; the known flaky loopback PWA network test passed on isolated rerun); website checks and 72 tests passed before deploy.
- DMG SHA-256: `c3bace84f0e44eae4439bac2ffd6881ca1d50683d38ad436b1d68c4818d33fa7`. `hdiutil verify` passed; bundled app reports version 1.27.0 (55); ad-hoc signature validation passed.
- GitHub latest release: https://github.com/techfanseric/ai-quota-bar/releases/tag/v1.27.0 ; DMG and SHA256SUMS.txt attached; GitHub asset digest matches the local DMG. Tag points to release commit `75647d0`.
- Website history synced to 45 published releases, with English and Chinese v1.27.0 content.
- Production Pages deployment: https://0977b3f1.ai-quota-bar.pages.dev (main). Verified the production `/`, `/en/`, `/changelog`, `/en/changelog` routes contain v1.27.0; `/v1/app-update` returns 1.27.0. Deploy required the documented API-token flow; api.cloudflare.com was unreachable directly during the session (TLS interception), so the deploy ran through the local proxy on 127.0.0.1:7897.
- Keychain prompt count verified on this Mac via securityd logs: exactly one prompt (own credential vault) per launch after install.
