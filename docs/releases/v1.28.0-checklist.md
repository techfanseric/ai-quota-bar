# v1.28.0 release record

- User authorized commit, push and formal release on 2026-09-24 ("把现在的版先推送和发布release"), ahead of a follow-up fix for MiniMax task-count accuracy.
- Version 1.28.0, macOS build 56; existing ad-hoc Apple Silicon distribution retained.
- Scope: "AI task protection" extended from Codex + Kimi to all four providers. Three new read-only passive detectors (2s polling, no message content read): GLM via the ZCode desktop/CLI shared session database (`~/.zcode/cli/db/db.sqlite`, interactive sessions fresh within 120s); MiniMax CLI via `~/.minimax/v2/sessions/**/manifest.json` `updatedAtMs`; and a Claude Code bridge attributing sessions to GLM or MiniMax by the responding model recorded in `~/.claude/projects/**/*.jsonl` transcripts (non-GLM/MiniMax models, e.g. the official Anthropic endpoint, never trigger protection). Coordinator merge/counts/UI generalized to four providers; presence gating per provider (Keychain credential or local client); menu status line lists detectable providers dynamically; power assertion renamed to the neutral "an AI task is working"; mobile dashboard active-provider mapping extended with glm/minimax.
- Validation: full Swift test suite passed (436 tests, 4 pre-existing skips, 0 failures), including 13 new tests (fixture-based tests for the three detectors plus coordinator merge/attribution/expiry tests).
- DMG SHA-256: `3f458b5686d3ab9f0b706c088e49e29163ca37a6b64d6974903cc451e97f608a`. `hdiutil verify` passed; bundled app reports version 1.28.0 (56); ad-hoc signature validation passed.
- GitHub latest release: https://github.com/techfanseric/ai-quota-bar/releases/tag/v1.28.0 ; DMG and SHA256SUMS.txt attached; GitHub asset digest matches the local DMG. Tag points to release commit `620901b`.
- Live behavior verified on this Mac: after `make install`, the new build detected the active ZCode session (GLM task) and held PreventUserIdleSystemSleep / PreventUserIdleDisplaySleep / UserIsActive assertions (`pmset -g assertions`).
- Website history synced to 46 published releases, with English and Chinese v1.28.0 content; homepage version strings updated to v1.28.0 (zh + en).
- Production Pages deployment: https://d88b26db.ai-quota-bar.pages.dev (main). Verified production `/`, `/en/`, `/changelog`, `/en/changelog` return 200 and contain v1.28.0; `/v1/app-update` returns 1.28.0 pointing at the v1.28.0 DMG. Website checks and 72 tests passed before deploy. Deploy used the documented node.cyber@gmail.com API token through the local proxy on 127.0.0.1:7897.
- Known issue carried forward (user-reported): MiniMax task count can be inaccurate and detection sometimes misses running tasks; freshness-window calibration and the day-folder scan limit are the suspects for the follow-up.
