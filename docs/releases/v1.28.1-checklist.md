# v1.28.1 release record

- User confirmed the fixes live ("好像已经没有问题了") and the release proceeded on 2026-09-24.
- Version 1.28.1, macOS build 57; existing ad-hoc Apple Silicon distribution retained.
- Scope: three reliability fixes on top of v1.28.0's four-provider task protection.
  - MiniMax: activity signal moved from `manifest.json` (measured minutes behind live transcript writes) to the newest file mtime inside each session directory; added the desktop MiniMax Code background-task store (`~/.minimax/background-tasks/bg_*`), previously invisible, as `minimax:bg:` sources labeled "MiniMax Code".
  - Kimi: the dead signals (`session_index.jsonl` unmaintained since Sep 5, wire files dormant since Sep 6, and the lsof cwd gate defeated by the desktop runtime's cwd `/`) were replaced by the desktop agent's `conversation-context-usage.json` per-conversation `updatedAt` plus CLI wire turn-state bounded by file-mtime freshness (`kimi:desktop:` / `kimi:cli:` labels).
  - Launch: `preloadCredentialVault` no longer blocks `applicationDidFinishLaunching` on the keychain ACL prompt (which froze the entire app — no status bar, no presence monitor — after every re-signed install). The preload runs fire-and-forget, corrects the persisted configured-providers cache, and posts `credentialVaultDidLoad`; StatusBarController refreshes once when it lands.
- Validation: full Swift test suite passed (439 tests, 4 pre-existing skips, 0 failures), including rewritten Kimi detector tests and new MiniMax background-task/stale-manifest regression tests. Live verification on this Mac: with a live bar, launching and quitting Kimi.app flipped the persisted collapsed-provider state (`['codex','minimax']` → Kimi added on quit), proving the presence → collapse → ring pipeline end to end.
- DMG SHA-256: `2639b8bf4c7dc0c34fe262b7800bc4702736793475424a5edde0c77d7135224f`. `hdiutil verify` passed; bundled app reports version 1.28.1 (57); ad-hoc signature validation passed.
- GitHub latest release: https://github.com/techfanseric/ai-quota-bar/releases/tag/v1.28.1 ; DMG and SHA256SUMS.txt attached; GitHub asset digest matches the local DMG. Tag points to release commit `b625d37`.
- Website history synced to 47 published releases, with English and Chinese v1.28.1 content; homepage version strings updated to v1.28.1 (zh + en).
- Production Pages deployment: https://ff1edd64.ai-quota-bar.pages.dev (main). Verified production `/`, `/en/`, `/changelog`, `/en/changelog` return 200 and contain v1.28.1; `/v1/app-update` returns 1.28.1 pointing at the v1.28.1 DMG. Website checks and 72 tests passed before deploy. Deploy used the documented node.cyber@gmail.com API token through the local proxy on 127.0.0.1:7897 (one earlier token-less invocation aborted harmlessly before upload).
- Notes: the keychain ACL prompt still appears once per re-signed dev install (ad-hoc signing has no stable identity); with this release it no longer freezes the app. The bar running on this Mac is the same build as the release DMG.
