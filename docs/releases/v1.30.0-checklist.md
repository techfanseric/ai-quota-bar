# v1.30.0 release record

- User authorized the release ("很好，先提交推送发布一个release并且按规范更新官网。") and it proceeded on 2026-09-26.
- Version 1.30.0, macOS build 59; existing ad-hoc Apple Silicon distribution retained.
- Commits: `2681f8c` (feat: shared Codex auth-file inspection in the local-usage core), `e41a909` (feat: login-free Codex account switching with per-account quota, includes the Makefile version bump per v1.29.0 convention), `78d80d2` (release notes). Tag `v1.30.0` points to the release commit `78d80d2`.
- Scope:
  - Right-click panel "Codex accounts" section: list active account + stashed backups (`~/.codex/accounts/`), login-free switching via atomic auth.json rename swap, log-in-new-account flow (stash current → reopen ChatGPT → user signs in), rename/delete (Trash) and one-click import of legacy hand-made `auth *.json` backups; same-account re-login replaces the older copy; API-key-mode files are never touched automatically.
  - Guard: the app never quits ChatGPT/Codex itself — switch requests park while the desktop app or a codex CLI process runs (node wrapper `@openai/codex` and native binary both detected; the `.codex` plugin extension host is filtered by `.regular` activation policy) and relay automatically after the user quits normally, with a final running-state re-check before any file moves. Completion notification + auto-reopen of ChatGPT.
  - Per-account last-known quota: new `CodexAccountQuotaStore` persists a snapshot (5h + Weekly remaining percent, reset times, capturedAt) keyed by account digest on every successful fetch; account rows render "5h 68%→16:44 · wk 41%→09/29" with full timestamps on hover.
  - Core: `CodexAuthFileReader` in CodexLocalUsageCore single-sources the login classification now shared by `UsageAccountObservation` and the switcher.
- Validation: full Swift suite 479 tests (4 pre-existing skips, 0 failures). Visual acceptance of offscreen panel renders ran five rounds; it caught and fixed a clipped "Log in a new account" row (chrome-height bug), quota-line truncation (compact format + `minimumScaleFactor` fallback) and an accounts-section max height too small for four accounts (250 → 280, panel total stays ≤ 850). Cloudflare `npm run check` and node tests passed before deploy.
- DMG SHA-256: `33d1e8eb29de0a065e15eab0cebfdcbc828ac6525a0275894f5e7e87f39bca16`. `hdiutil verify` passed; mounted DMG reports version 1.30.0 (59); `codesign --verify --deep --strict` passed; SHA256SUMS.txt round-trip checked.
- GitHub latest release: https://github.com/techfanseric/ai-quota-bar/releases/tag/v1.30.0 ; DMG and SHA256SUMS.txt attached; created with the zh title/body per convention. First push attempt hung once ("remote end hung up unexpectedly"); the retry pushed `3e00be7..78d80d2`.
- Website history synced to 49 published releases via `npm run sync:releases`, with English keys (`name_en`/`body_en`) added for v1.30.0; homepage version strings updated to v1.30.0 (zh + en, hero/download buttons + release-note lines).
- Production Pages deployment: https://15867648.ai-quota-bar.pages.dev (main). Verified production `/`, `/en/`, `/changelog`, `/en/changelog` return 200 and contain v1.30.0; `/v1/app-update` returns 1.30.0 pointing at the v1.30.0 DMG. Deploy used the documented node.cyberic@gmail.com API token through the local proxy on 127.0.0.1:7897.
- Notes: the Mac's installed app predates the version bump (installed mid-development via `make install`); `make install` picks up 1.30.0/59 when desired (one-time keychain ACL prompt per re-signed install, non-blocking since v1.28.1).
