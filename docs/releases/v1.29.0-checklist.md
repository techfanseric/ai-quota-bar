# v1.29.0 release record

- User authorized the release ("还没提交的代码可以提交推送发布到生产并发新release了") and it proceeded on 2026-09-24.
- Version 1.29.0, macOS build 58; existing ad-hoc Apple Silicon distribution retained.
- Branch handling: the working tree carried this release's work on `windows/kickoff`; it was stashed, landed on `main` as five commits (303007d, 113095c, 24162bc, e2745b7, 87171da), and released from `main` per convention. The Windows-port commits remain only on `windows/kickoff`.
- Scope:
  - GLM/ZCode: hybrid task-end detection — `turn_usage` terminal rows (completed/error/cancelled, rowid-cursored) immediately end a session's task wave; freshness still drives start/running; schema drift falls back to freshness-only. End/cancel latency ~120s → one poll cycle (~2–3s).
  - Kimi CLI: incremental wire parsing from the last consumed newline (idle machines re-read hundreds of transcripts per poll before, pinning a core).
  - MiniMax: bg_* directory mtimes prefetched with the listing + raw POSIX stat for probe files (FileManager ObjC exception churn over thousands of folders saturated a core).
  - Menu bar: new "chase queue" task-wave layout (new default; evenly spaced stays selectable), waves queue on the visible arc with constant-bright head beads; ambient animation re-render cadence 30fps → 12fps; Reduce Motion parks a static queue pose.
  - Website/admin: `POST /v1/admin/team-session` mints an OPS_ADMIN_SECRET-signed manager cookie; /admin "Open dashboard" enters any team (device-only teams included, overview synthesizes the team row); invite rotation on device-only teams returns 404 instead of an unpersisted code.
- Validation: full Swift suite passed (458 tests, 4 pre-existing skips, 0 failures). `npm run check` + 73 node tests passed before deploy. Release binary verified to contain the new chase-queue code.
- DMG SHA-256: `e6c7aff633989b5a229a5d899e6d61bddec1b7bcd10e20c91727bed83b4f6a03`. `hdiutil verify` passed; bundled app reports version 1.29.0 (58); `codesign --verify --deep --strict` passed.
- GitHub latest release: https://github.com/techfanseric/ai-quota-bar/releases/tag/v1.29.0 ; DMG and SHA256SUMS.txt attached; GitHub asset digest matches the local DMG. Tag points to release commit `aa1819e`.
- Website history synced to 48 published releases, with English and Chinese v1.29.0 content; homepage version strings updated to v1.29.0 (zh + en).
- Production Pages deployment: https://d3104f3d.ai-quota-bar.pages.dev (main). Verified production `/`, `/en/`, `/changelog`, `/en/changelog` return 200 and contain v1.29.0; `/v1/app-update` returns 1.29.0 pointing at the v1.29.0 DMG. Deploy used the documented node.cyberic@gmail.com API token through the local proxy on 127.0.0.1:7897.
- Notes: the app running on this Mac was not reinstalled during the release; `make install` picks up build 58 when desired (one-time keychain ACL prompt per re-signed install, non-blocking since v1.28.1).
