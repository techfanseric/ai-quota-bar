{
  "name": "ai-quota-bar-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/legacy-quota.js && node --check src/team-quota.js && node --check src/worker.js && node --check src/local-usage.js && node --check src/team.js && node --check src/usage-team-core.js && node --check src/feedback.js && node --check public/demo.js && node --check src/operations.js && node --check public/admin.js && node --check public/team.js && node --check public/feedback.js && node --check public/changelog.js && node --check public/cycle-demo.js",
    "test": "node --experimental-sqlite --test tests/*.test.js",
    "deploy": "npm run deploy:pages",
    "db:migrate": "wrangler d1 execute ai-quota-bar --file=./schema.sql",
    "test:usage-http": "node --experimental-sqlite tests/local-usage-http.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "deploy:pages": "npm run build:pages && wrangler pages deploy dist-pages --project-name ai-quota-bar --branch main",
    "sync:releases": "node scripts/sync-releases.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "marked": "^18.0.13",
    "wrangler": "^4.15.0"
  }
}
